import * as React from "react";
import AppBar from "../Components/AppBar";
import Box from '@mui/material/Box';

function EventPage () {
   
   return(
      <Box>
         <AppBar PageName = {"Event's Page"}/>
         
      </Box>
   );
}

export default EventPage